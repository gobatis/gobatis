<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//gobatis.co//DTD Mapper 1.0//EN" "gobatis.co/dtd/mapper.dtd">
<mapper>
    <insert id="InsertE001" parameter="member">
        insert into members(
        username,
        email,
        mobile,
        password,
        status
        ) values(
        #{member.Username},
        #{member.email},
        #{member.mobile},
        #{member.password},
        #{member.status}
        )
    </insert>

    <select id="InsertE002" parameter="member" result="id">
        insert into members(
        username,
        email,
        mobile,
        password,
        status
        ) values(
        #{member.Username},
        #{member.email},
        #{member.mobile},
        #{member.password},
        #{member.status}
        ) returning id;
    </select>

    <insert id="InsertE011" parameter="member">
        <inserter table="members">
            <field name="*"/>
        </inserter>
    </insert>

    <select id="InsertE012" parameter="member" result="id">
        <inserter table="members">
            <field name="*"/>
        </inserter>
        returning id;
    </select>
</mapper>